# Screenshots

Drop screenshots here and reference them from the README, e.g.:

- `floating-card-compact.png` — the compact floating card with balance / total tokens / model calls
- `dashboard-expanded.png` — the expanded dashboard: balance cards, token stats, 14-day stacked bar chart, per-model chart, top sessions
- `sidebar-button.png` — the "用量" button at the sidebar foot

Suggested naming: `*.png`, landscape, ~1200 px wide.
